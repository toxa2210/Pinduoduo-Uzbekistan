import { BadRequestException,Injectable } from "@nestjs/common"; import { PrismaService } from "../../database/prisma.service"; @Injectable() export class OrdersService {constructor(private readonly prisma:PrismaService){} async create(input:{userId:string;deliveryAddress:string;items:Array<{productId:string;quantity:number}>}){if(!input.items.length)throw new BadRequestException("Order must contain at least one item");const ids=input.items.map(i=>i.productId),products=await this.prisma.product.findMany({where:{id:{in:ids},status:"ACTIVE"}});if(products.length!==new Set(ids).size)throw new BadRequestException("One or more products are unavailable");const byId=new Map(products.map(p=>[p.id,p]));const totalMinor=input.items.reduce((sum,i)=>{if(!Number.isInteger(i.quantity)||i.quantity<1||i.quantity>999)throw new BadRequestException("Invalid product quantity");return sum+byId.get(i.productId)!.priceMinor*i.quantity;},0);return this.prisma.order.create({data:{userId:input.userId,deliveryAddress:input.deliveryAddress,totalMinor,currency:"UZS",items:{create:input.items.map(i=>({productId:i.productId,quantity:i.quantity,unitPriceMinor:byId.get(i.productId)!.priceMinor}))}},include:{items:true}});} listForUser(userId:string){return this.prisma.order.findMany({where:{userId},orderBy:{createdAt:"desc"},include:{items:true}});}}