import { Injectable,NotFoundException } from "@nestjs/common"; import { PrismaService } from "../../database/prisma.service"; @Injectable() export class CatalogService { constructor(private readonly prisma:PrismaService){} listCategories(){return this.prisma.category.findMany({orderBy:{nameUz:"asc"}});} async getProduct(id:string){const product=await this.prisma.product.findFirst({where:{id,status:"ACTIVE"},include:{category:true}});if(!product)throw new NotFoundException("Product not found");return product;} async listProducts(p:{categoryId?:string;search?:string;page?:number;limit?:number}){const page=Math.max(p.page??1,1),limit=Math.min(Math.max(p.limit??20,1),100);const where={status:"ACTIVE" as const,...(p.categoryId?{categoryId:p.categoryId}:{}),...(p.search?{OR:[{titleUz:{contains:p.search,mode:"insensitive" as const}},{titleRu:{contains:p.search,mode:"insensitive" as const}}]}:{})};const [items,total]=await Promise.all([this.prisma.product.findMany({where,orderBy:{createdAt:"desc"},skip:(page-1)*limit,take:limit}),this.prisma.product.count({where})]);return {items,page,limit,total,pages:Math.ceil(total/limit)};}}